describe("parser", () => {
  it.todo("renames $value to value");
  it.todo("renames $description to comment");
  it.todo("converts composite token into a group token"); // doesn't have a "value"
  it.todo("expands composite token into individual tokens"); // so SD can transform them
  it.todo("creates new composite token"); // $value properties replaced with aliases / has all original properties
  it.todo("handles no tokens without error");
});
