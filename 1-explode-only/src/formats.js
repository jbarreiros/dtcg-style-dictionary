exports.cssUtilityClass = {
  name: "css/utility-class",
  formatter: ({ dictionary, file, options }) => {
    console.log("format", dictionary.tokens.composite.border.thin);

    // const class = []

    // class.push()

    return "";
  },
};
